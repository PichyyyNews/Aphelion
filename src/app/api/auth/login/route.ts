import {NextResponse} from 'next/server'; import bcrypt from 'bcryptjs'; import {db} from '@/lib/db'; import {createSession} from '@/lib/auth';
export async function POST(req:Request){const {email,password}=await req.json();const u=await db.user.findUnique({where:{email}});if(!u||!u.password||!(await bcrypt.compare(password,u.password)))return NextResponse.json({error:'Invalid email or password'},{status:401});if(u.status==='BANNED')return NextResponse.json({error:'Account disabled'},{status:403});const token=await createSession(u.id);const r=NextResponse.json({ok:true});r.cookies.set('aphelion_session',token,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:604800,path:'/'});return r;}
