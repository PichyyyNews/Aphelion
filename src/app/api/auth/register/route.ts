import {NextResponse} from 'next/server'; import {z} from 'zod'; import bcrypt from 'bcryptjs'; import {db} from '@/lib/db'; import {createSession} from '@/lib/auth';
const schema=z.object({email:z.string().email(),password:z.string().min(8),name:z.string().optional()}); export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid input'},{status:400});if(await db.user.findUnique({where:{email:p.data.email}}))return NextResponse.json({error:'Email already registered'},{status:409});const u=await db.user.create({data:{email:p.data.email,password:await bcrypt.hash(p.data.password,12),name:p.data.name}});const token=await createSession(u.id);const r=NextResponse.json({ok:true});r.cookies.set('aphelion_session',token,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:604800,path:'/'});return r;}
