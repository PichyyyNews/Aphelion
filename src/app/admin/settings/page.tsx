'use client';
import {useState} from 'react'; import {Button} from '@/components/ui/button'; import {Card,CardContent,CardDescription,CardFooter,CardHeader,CardTitle} from '@/components/ui/card'; import {Field,FieldGroup,FieldLabel} from '@/components/ui/field'; import {Input} from '@/components/ui/input'; import {PasswordInput} from '@/components/password-input';
export default function Settings(){const[provider,setProvider]=useState<'github'|'google'>('github'),[enabled,setEnabled]=useState(true),[clientId,setClientId]=useState(''),[clientSecret,setClientSecret]=useState(''),[message,setMessage]=useState('');async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/oauth',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({provider,enabled,clientId,clientSecret})});setMessage(r.ok?'Saved. OAuth is ready for testing.':'Unable to save settings.')}return <main className="mx-auto max-w-xl px-5 py-16"><form onSubmit={save}><Card><CardHeader><CardTitle>OAuth providers</CardTitle><CardDescription>Set callback URL to <code>/api/auth/oauth/[provider]/callback</code>.</CardDescription></CardHeader><CardContent><FieldGroup><Field><FieldLabel htmlFor="provider">Provider</FieldLabel><select id="provider" className="h-9 rounded-lg border bg-background px-3" value={provider} onChange={e=>setProvider(e.target.value as 'github'|'google')}><option value="github">GitHub</option><option value="google">Google</option></select></Field><Field><FieldLabel htmlFor="client-id">Client ID</FieldLabel><Input id="client-id" value={clientId} onChange={e=>setClientId(e.target.value)} required/></Field><Field><FieldLabel htmlFor="client-secret">Client secret</FieldLabel><PasswordInput id="client-secret" value={clientSecret} onChange={e=>setClientSecret(e.target.value)} required/></Field><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> Enable this provider</label></FieldGroup></CardContent><CardFooter className="flex-col items-stretch gap-2"><Button type="submit">Save provider</Button>{message&&<p aria-live="polite" className="text-sm text-muted-foreground">{message}</p>}</CardFooter></Card></form></main>}
