import { buttonVariants } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/components/ui/card';

export default function Home(){return <main className="mx-auto flex max-w-5xl px-5 py-20 sm:px-8"><Card className="w-full"><CardHeader><p className="text-xs font-medium tracking-[0.18em] text-muted-foreground">APHELION AUTH SYSTEM</p><CardTitle className="text-3xl sm:text-5xl">Identity infrastructure for the next orbit.</CardTitle><CardDescription className="max-w-2xl text-base">A clean, containerized authentication foundation with admin controls and room to scale.</CardDescription></CardHeader><CardContent className="grid gap-3 sm:grid-cols-3"><div className="rounded-lg border p-4"><p className="font-medium">Private by default</p><p className="mt-1 text-sm text-muted-foreground">Sessions and credentials stay protected.</p></div><div className="rounded-lg border p-4"><p className="font-medium">Container ready</p><p className="mt-1 text-sm text-muted-foreground">App, PostgreSQL and Redis are isolated.</p></div><div className="rounded-lg border p-4"><p className="font-medium">Admin aware</p><p className="mt-1 text-sm text-muted-foreground">A focused control surface for access.</p></div></CardContent><CardFooter className="gap-3"><a className={buttonVariants()} href="/login">Sign in</a><a className={buttonVariants({variant:'outline'})} href="/register">Create account</a></CardFooter></Card></main>}
