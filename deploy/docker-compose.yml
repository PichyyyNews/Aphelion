version: '3.8'

services:
  # --- FRONTEND & GATEWAY ---
  api-gateway:
    image: envoyproxy/envoy:v1.30-latest
    volumes:
      - ./gateway/envoy.yaml:/etc/envoy/envoy.yaml
    ports:
      - "80:10000"
    depends_on:
      - web-frontend
      - svc-user-auth
      - svc-activity-feed
      - svc-notification

  web-frontend:
    build:
      context: ../apps/web-frontend
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://user:pass@db-core:5432/user_db?schema=public
      - AUTH_SECRET=dev-secret-change-me
      - ADMIN_PANEL_PATH=control-room
      - ADMIN_PANEL_PASSWORD=AdminChangeMe123!
      - INITIAL_ADMIN_EMAIL=admin@aphelion.local
      - INITIAL_ADMIN_PASSWORD=ChangeMe123!
      - APP_URL=http://localhost
    depends_on:
      - db-core
      - infra-cache

  # --- BACKEND MICROSERVICES ---
  svc-user-auth:
    build:
      context: ../apps/svc-user-auth
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://user:pass@db-core:5432/user_db
      - REDIS_URL=redis://infra-cache:6379/0
      - AUTH_SECRET=dev-secret-change-me
      - ENCRYPTION_KEY=dev-secret-change-me
      - ADMIN_PANEL_PASSWORD=AdminChangeMe123!
      - INITIAL_ADMIN_EMAIL=admin@aphelion.local
      - INITIAL_ADMIN_PASSWORD=ChangeMe123!
      - APP_URL=http://localhost
    depends_on:
      - db-core
      - infra-cache

  svc-activity-feed:
    build:
      context: ../apps/svc-activity-feed
      dockerfile: Dockerfile
    environment:
      - PORT=3002
      - KAFKA_BROKERS=infra-kafka:9092
      - DB_SCYLLA_HOSTS=db-activity
    depends_on:
      - db-activity
      - infra-kafka

  svc-notification:
    build:
      context: ../apps/svc-notification
      dockerfile: Dockerfile
    environment:
      - PORT=8081
      - KAFKA_BROKERS=infra-kafka:9092
    depends_on:
      - infra-kafka

  # --- CACHE & BROKER ---
  infra-cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  infra-kafka:
    image: confluentinc/cp-kafka:7.5.0
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAYGROUND:PLAINTEXT'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://infra-kafka:9092,PLAYGROUND://localhost:29092'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@infra-kafka:29093'
      KAFKA_LISTENERS: 'PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:29093,PLAYGROUND://0.0.0.0:29092'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAME: 'CONTROLLER'
      KAFKA_LOG_DIRS: '/tmp/kraft-combined-logs'
      CLUSTER_ID: 'MkU3OEVBNTcwNTJENDM2Qk'

  # --- DATABASES ---
  db-core:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: user_db
    volumes:
      - ./database/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"

  db-activity:
    image: scylladb/scylla:5.4.0
    volumes:
      - ./database/scylla-init.cql:/scylla-init.cql
    ports:
      - "9042:9042"

  db-social-graph:
    image: neo4j:5.12-community
    environment:
      NEO4J_AUTH: neo4j/password
    ports:
      - "7474:7474"
      - "7687:7687"
